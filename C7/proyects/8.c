#include <stdio.h>
int main(){
	int h, m;
	int t;
	printf("Enter a time: ");
	scanf("%d:%d", &h, &m);

	t = (h * 60) + m;
	int dep;
	char arr;
	
	t = dep;
	switch (dep){
	case (dep < 480 ): arr = '10:16 am';

	}
}

